/* 
  document.getElementById("login").addEventListener("submit", function(event) {
    event.preventDefault(); // Evitar el envío del formulario
    const Email = document.getElementById("p_email").value;
    const password = document.getElementById("p_pass").value;

    // Aquí agregarías la lógica de autenticación con el servidor (por ejemplo, una solicitud AJAX)
    // Supongamos que se ha hecho la autenticación correctamente:
    if (Email === "p_email" && password === "p_pass") {
      document.getElementById("contenido-inicial").style.display = "none";
      document.getElementById("contenido-despues-login").style.display = "block";
    }
  }); */

